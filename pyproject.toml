[project]
name = "sqlalchemy-lsp-e2e"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-lsp>=1.0.0",
    "lsprotocol>=2023.0.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests/e2e"]
log_cli = false
